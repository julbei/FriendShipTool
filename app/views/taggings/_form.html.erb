<%= simple_form_for(@tagging) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :tag %>
    <%= f.association :accounting %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
